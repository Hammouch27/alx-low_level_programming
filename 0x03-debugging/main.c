#include "main.h"

/**
 * main - Tests the positive_or_negative function
 *
 * This function is the entry point of the program. It tests the positive_or_negative function
 * by calling it with the integer value of 98. The result of the function is printed to the console.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        int i;

        i = 98;
        positive_or_negative(i);

        return (0);
}
