#include "main.h"

/**
 * main - Test function for positive or negative
 *
 * This function calls the positive_or_negative function to test the given integer.
 * It initializes `i` to 0 and passes it to the positive_or_negative function.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int i;

    i = 0;
    positive_or_negative(i);
    return (0);
}

